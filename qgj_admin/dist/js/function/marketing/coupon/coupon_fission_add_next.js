/*
 *  优惠券裂变券下一步
 * */

$(function () {
    //颜色选择
    util.setColor({
        target: '.js-coupon-set-items', //选择后颜色作用的对象
        element: '.color-set',   //颜色选取对象
        el_reset:'.reset-color',  //重置颜色对象
    });
});